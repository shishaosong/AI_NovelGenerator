# manifest.yml
---
applications:
- name: novel                # 应用名称
  memory: 4G                # 内存大小
  disk_quota: 100G            # 磁盘空间配额
  instances: 1
  random-route: true
  buildpack: python_buildpack
  command: streamlit run main.py --server.port $PORT --server.headless true
